---
description: Write test cases from what-to-build.md, run this repo's unit/integration/e2e/security tests, and record whether the feature is slop. After feature-code-reviewer on the daily path.
alwaysApply: false
---

# Skill: feature-verifier

Read and follow `skills/feature-verifier/SKILL.md`. Fill `skills/feature-verifier/templates/what-was-verified.md` only.

Announce: `Using skill: feature-verifier`.

## Job

Prove the implementation matches `what-to-build.md`. Write real tests, run the commands this repo already has, report pass/fail.

## Must (order)

1. Read `what-to-build.md` and the shipped code.
2. Add tests for done-when, no-path, leave/cancel, and at least one wall. No hollow asserts.
3. Run existing unit, integration, e2e, and security commands. If a layer is missing, write `absent`. If Playwright / `test:e2e` exists, run it for UI work. Do not invent CI or a framework. No `&&` on Windows PowerShell.
4. Write `docs/features/<name>/what-was-verified.md`.

## Must not

- New product
- New test runner, CI, or scanner vendor
- Threat-model essay
- Claim pass without a command result or a repeatable manual check
