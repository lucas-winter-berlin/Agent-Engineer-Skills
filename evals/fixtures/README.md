# Quality-eval fixtures

Tiny consuming projects. The quality runner copies one of these, runs `git init`, and injects a single skill. Not copied into apps.

| Folder | Use |
| --- | --- |
| `toy-web/` | Existing app (specifier, developer, reviewer, tester, mvp refuse) |
| `greenfield/` | Empty workspace (mvp-specifier pitch cases) |
