# Toy Web

A tiny existing storefront used by Agent Engineer Skills quality evals.

Signed-in customers can see their orders. Pages live in `public/`. Helpers live in `src/`. There is no test runner, no CI, and no extra packages.
